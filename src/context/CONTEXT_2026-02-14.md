# Context: 2026-02-14 Constants Refactor

## Goal
To refactor duplicate or magic numeric literals into a canonical constants file.
Specifically targeting `4173` (default dashboard port) and `1500` (race check delay).

## Scope
- Create `src/constants.mjs`.
- Modify `src/lib.mjs`.

## Constraints
- Must preserve existing values.
- Must pass lint and tests.
- Must follow `const-refactor-agent` guidelines.
