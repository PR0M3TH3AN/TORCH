{
  "raw_output": "src/relay-health.mjs:    timeoutId = setTimeout(() => reject(new Error(message)), timeoutMs);\nsrc/test_logs/TEST_LOG_2026-02-17T00-00-00Z.md:- Command: grep -rE \"setInterval|setTimeout\" src/\nsrc/lib.mjs:    await new Promise((resolve) => setTimeout(resolve, raceCheckDelayMs));\nsrc/prompts/daily/const-refactor-agent.md:     - Timeout: look for `setTimeout`, `fetch(..., { timeout: ...})`, `pool.list` timeouts, `BACKGROUND_RELAY_TIMEOUT_MS`.\nsrc/prompts/daily/perf-agent.md:- `setInterval|setTimeout|requestAnimationFrame|requestIdleCallback`\nsrc/prompts/daily/test-audit-agent.md:     - Long sleeps: `setTimeout(`, `sleep(`, `await delay(`.\nsrc/prompts/daily/test-audit-agent.md:  - `rg \"setTimeout\\\\(|sleep\\\\(|await delay\\\\(|new Promise\\\\(r => setTimeout\" -n`\nsrc/prompts/daily/test-audit-agent.md:  1. **Test-fix PR** — add missing assertions, replace `setTimeout` sleeps with `waitFor`, or mock timers. Include `src/context/CONTEXT_<timestamp>.md`, `src/todo/TODO_<timestamp>.md`, `src/test_logs/TEST_LOG_<timestamp>.md`.\nsrc/prompts/daily/test-audit-agent.md:- Replace `await new Promise(r => setTimeout(r, 1000))` with a deterministic `await waitFor(() => condition)` or mock timers.\nsrc/cmd-lock.mjs:    await new Promise((resolve) => setTimeout(resolve, RACE_CHECK_DELAY_MS));\nsrc/lock-ops.mjs:    timeoutHandle = setTimeout(() => reject(new Error(timeoutMessage)), timeoutMs);\nsrc/lock-ops.mjs:    sleepFn = (ms) => new Promise((resolve) => setTimeout(resolve, ms)),\nsrc/services/memory/scheduler.js:  const handle = setInterval(() => {\nsrc/services/memory/scheduler.js:          setTimeout(resolve, options.retryDelayMs);\nsrc/services/memory/scheduler.js:    const intervalHandle = setInterval(run, definition.intervalMs);\nsrc/services/memory/scheduler.js:      const timeout = setTimeout(async () => {"
}
