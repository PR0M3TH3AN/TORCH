# Memory Update — style-agent — 2026-02-21

## Key findings
- `format` script is missing in `package.json`, preventing automated formatting.
- `lint:inline-styles` is missing, but required by prompt.

## Warnings / gotchas
- `npm run format` will fail until the script is added.
