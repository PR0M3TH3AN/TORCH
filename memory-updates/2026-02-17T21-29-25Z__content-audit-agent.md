# Learnings - 2026-02-17

- The `content-audit-agent` is configured to audit the `/content` directory, but this directory does not exist in the repository root.
- Added `content-audit-agent targets missing /content directory` to `KNOWN_ISSUES.md`.
- No audit actions were performed.
