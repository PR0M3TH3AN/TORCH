# Memory Update — smoke-agent — 2026-02-22

## Key findings
- `npm test` passed cleanly in the sandbox environment today, despite known issues warning about EPERM.

## Patterns / reusable knowledge
- `nostr-tools` must be installed (`npm install`) if missing for lock backend tests to run.

## Warnings / gotchas
- Ensure environment dependencies are present before running integration tests.
